# Vortex generation benchmarks

Every evolution generation is compared against the previous last-known-good overlay.

- Fixtures: `tests/fixtures/golden_tasks.json`
- Golden outputs: `tests/golden_outputs/baseline_v000.json`
- Live results: `~/.vortex/releases/vXXX/golden/benchmark.json`

Promotion requires quality ↑, reliability not down, security pass, latency within limit, cost within limit, regressions = 0.
